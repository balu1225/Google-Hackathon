# Copy this file to .env and fill in your values
# .env is gitignored — never commit real credentials

# MongoDB Atlas connection string
# Replace <db_password> with your Atlas database user password
MONGODB_ATLAS_URI=mongodb+srv://balu:<db_password>@cluster0.8oqcf0i.mongodb.net/fraudshield?retryWrites=true&w=majority&appName=Cluster0

# Google Cloud project (for Vertex AI / Gemini)
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# Frontend backend URL (for local dev, keep as localhost)
VITE_BACKEND_URL=http://localhost:8080
